---
"@aichatctl/sdk": minor
"aichatctl": minor
---

Add `notebook status`: report the state of a NotebookLM notebook's Studio artifacts (Audio Overviews today) with a best-effort `type`/`state`, so consumers can poll until an Audio Overview is ready.
